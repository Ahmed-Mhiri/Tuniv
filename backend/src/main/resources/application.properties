# ===============================
# DATABASE CONFIGURATION
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/tuniv_db
spring.datasource.username=postgres
spring.datasource.password=myLocalDbPass123


# Storage Configuration (local or s3)
storage.type=local

# Local Storage Directory
file.upload-dir=./uploads


app.frontend.url=http://localhost:4200


# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
# 'update' is good for development. For production, consider 'validate' or a migration tool like Flyway.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# SECURITY / JWT CONFIGURATION
# ===============================
jwt.secret=dGhpcy1pcy1teS1zdXBlci1zZWNyZXQta2V5LWZvci10aGUtdHVuaXYtcHJvamVjdC0yMDI1

# ===============================
# EMAIL (SMTP) CONFIGURATION
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ahmedmhiri2004@gmail.com
spring.mail.password=pncm ovjr uhvm kfkv

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# ===============================
# DEBUGGING
# ===============================
logging.level.org.springframework.security=DEBUG


# ===============================
# CLOUD FILE STORAGE (S3) CONFIGURATION
# ===============================
# These can be placeholder values for now, but they must exist.
aws.s3.bucket-name=your-bucket-name
aws.s3.region=us-east-1
aws.s3.endpoint=https://s3.us-east-1.amazonaws.com
aws.access.key=your-access-key
aws.secret.key=your-secret-key

# Caching Configuration
spring.cache.type=simple
# ===============================
# REDIS CACHE CONFIGURATION
# ===============================
spring.data.redis.host=localhost
spring.data.redis.port=6379
# Spring Multipart (File Upload) Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

